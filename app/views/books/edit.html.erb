<h1>Editing Book</h1>

<%= render 'form', book: @book %>

<%= link_to 'Show', @book %> |
<%= link_to 'Delete', @book, method: :delete, data: { confirm: "Are you sure you want to delete #{@book.title}?" } %> |
<%= link_to 'Back', books_path %>
